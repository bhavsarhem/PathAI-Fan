# Optional — only needed for real Gemini LLM chat
GEMINI_API_KEY=your_gemini_api_key_here

# JWT secret for production auth
JWT_SECRET=pathai_demo_secret_2026

# BigQuery (optional — BigQuery client gracefully skips if absent)
BIGQUERY_PROJECT_ID=your_gcp_project_id
BIGQUERY_DATASET=pathai_fifa2026
